import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary   = Color(0xFF7C3AED); // violet
  static const secondary = Color(0xFFDB2777); // pink

  // Background layers
  static const bg        = Color(0xFF09090B);
  static const bg2       = Color(0xFF0F172A);
  static const bg3       = Color(0xFF18181B);

  // Borders
  static const border    = Color(0xFF27272A);
  static const border2   = Color(0xFF3F3F46);

  // Text
  static const text      = Color(0xFFFAFAFA);
  static const text2     = Color(0xFFA1A1AA);
  static const text3     = Color(0xFF52525B);
  static const text4     = Color(0xFF71717A);

  // Aura badge colours
  static const sprout    = Color(0xFF059669);
  static const spark     = Color(0xFFD97706);
  static const flame     = Color(0xFFEA580C);
  static const voltage   = Color(0xFF7C3AED);
  static const nova      = Color(0xFFDB2777);

  // Semantic
  static const like      = Color(0xFFE11D48);
  static const repost    = Color(0xFF059669);
  static const solved    = Color(0xFF059669);
}
